{
  "name": "shapes-generator",
  "scripts": {
    "prestart": "[ -d node_modules ] || npm install",
    "start": "polymer serve --hostname 0.0.0.0 --port 8081"
  },
  "dependencies": {
    "@polymer/polymer": "^3.0.0",
    "jszip": "^3.1.5"
  },
  "devDependencies": {
    "@webcomponents/webcomponentsjs": "^2.0.0",
    "wct-browser-legacy": "^1.0.0"
  }
}
